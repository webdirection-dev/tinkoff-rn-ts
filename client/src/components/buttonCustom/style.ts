import {StyleSheet} from "react-native"
import {gray100, yellow500} from "../../static/styles/var"

export const stylesButton = StyleSheet.create({
    button: {
        width: '100%',
        borderRadius: 15,
        marginTop: 15,
        padding: 15,
        backgroundColor: yellow500
    },
})
